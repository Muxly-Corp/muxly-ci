name: Test matrix in reusable workflow

on:
  workflow_call:

jobs:
  stub:
    runs-on: ubuntu-latest
    steps:
      - run: echo "stub ran"

  matrix-test:
    needs: stub
    strategy:
      fail-fast: false
      matrix:
        cluster: [dev-ru, dev-eu]
    runs-on: muxly-${{ matrix.cluster }}
    steps:
      - run: echo "matrix.cluster=${{ matrix.cluster }}"
